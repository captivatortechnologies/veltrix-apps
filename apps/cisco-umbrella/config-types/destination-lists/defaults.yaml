# Defaults for a new destination list item. A new list starts as a block list
# with tenant (non-global) scope; the operator fills in the name and the
# destinations. FLAT { fieldKey: value } — an "item" template takes flat defaults.
access: "block"
isGlobal: false
