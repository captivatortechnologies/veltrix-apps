import { deploymentGetStatus } from '../../lib/deployments'

/** Deployment status for an internal-network-policy-assignments configuration, from platform records. */
export default deploymentGetStatus
