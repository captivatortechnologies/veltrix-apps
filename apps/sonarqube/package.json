{
  "name": "veltrix-app-sonarqube",
  "private": true,
  "version": "0.4.0",
  "description": "Veltrix app: manage SonarQube quality gates, quality profiles, rule overrides, webhooks, permission templates, global settings, new code periods, global permissions and ALM (DevOps Platform) settings as code over the SonarQube Web API, plus BYOL infrastructure provisioning for the SonarQube stack",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@veltrixsecops/app-sdk": "^3.7.0",
    "fastify": "^5.0.0",
    "typescript": "^5.6.0"
  }
}
