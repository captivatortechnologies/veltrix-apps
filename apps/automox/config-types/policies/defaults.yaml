# Defaults for a new Policy item. Intentionally minimal: the canvas fields
# already declare their own defaultValue (policy_type_name: patch,
# schedule_time: "00:00", auto_patch/auto_reboot/notify_user/notify_reboot_user:
# true, etc.) — anything set here would be a FLAT map keyed by the canvas field
# keys (see config-types/policies/canvas.yaml), applied when a user adds a new
# item.
