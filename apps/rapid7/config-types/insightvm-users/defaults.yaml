# Defaults for a new user item.
#
# Intentionally empty: the platform no longer seeds system default values into a
# new item — it starts blank. Any values here would be a flat map keyed by the
# canvas field keys (login, name, email, enabled, password,
# password_reset_on_login, role_id, all_sites, site_names, all_asset_groups,
# asset_group_names, superuser, auth_source_type, auth_source_id).
