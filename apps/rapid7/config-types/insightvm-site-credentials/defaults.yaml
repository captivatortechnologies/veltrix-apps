# Defaults for a new site credential item.
#
# Intentionally empty: the platform no longer seeds system default values into a
# new item — it starts blank. Any values here would be a flat map keyed by the
# canvas field keys (site_name, name, description, credential_json, secret).
# The secret is write-only and must never be given a default.
