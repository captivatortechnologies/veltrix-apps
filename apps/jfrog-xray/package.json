{
  "name": "veltrix-app-jfrog-xray",
  "private": true,
  "version": "0.2.0",
  "description": "Veltrix app: manage JFrog Xray (software composition analysis / supply-chain security) configuration as code — security policies, license policies, watches and ignore rules — over the Xray REST API",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^20.0.0",
    "@veltrixsecops/app-sdk": "^3.7.0",
    "fastify": "^5.0.0",
    "typescript": "^5.6.0"
  }
}
