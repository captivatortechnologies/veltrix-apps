# Defaults for a new webhook item — intentionally blank; the item starts empty
# and the operator fills in name/url. Any values here would be a FLAT map
# keyed by the canvas field keys (name, description, url, user_name, password,
# use_proxy, headers).
