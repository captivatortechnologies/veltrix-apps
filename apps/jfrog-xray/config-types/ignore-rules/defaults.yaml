# Defaults for a new ignore-rule item — intentionally blank; the item starts
# empty and the operator fills in notes + at least one objective filter. Any
# values here would be a FLAT map keyed by the canvas field keys (notes,
# expires_at, vulnerability_ids, cve_ids, license_names, watch_names,
# policy_names, component_names, git_repository_names, docker_layer_shas,
# additional_filters_json).
