{
  "name": "veltrix-app-aqua-security",
  "private": true,
  "version": "0.1.0",
  "description": "Veltrix app: manage Aqua Security as code — image, host, function and Kubernetes assurance policies, container and host runtime policies, firewall policies, application scopes and enforcer groups — over the Aqua CSP/Enterprise Console REST API",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@veltrixsecops/app-sdk": "^3.7.0",
    "fastify": "^5.0.0",
    "typescript": "^5.6.0"
  }
}
