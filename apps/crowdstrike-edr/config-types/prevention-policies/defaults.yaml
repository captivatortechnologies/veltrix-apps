# Default values seeded into every new prevention policy item on the canvas.
# Flat mapping: field key → default value (the canvas declares one `item`).

name: ""
platform: "Windows"
description: ""
enabled: false
hostGroups: ""
settings: |
  [
    { "id": "NextGenAV", "value": { "enabled": true } },
    { "id": "CloudAntiMalware", "value": { "detection": "MODERATE", "prevention": "MODERATE" } },
    { "id": "OnSensorMLSlider", "value": { "detection": "MODERATE", "prevention": "MODERATE" } }
  ]
