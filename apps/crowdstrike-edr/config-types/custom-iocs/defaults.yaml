# Default values seeded into every new indicator item on the canvas.
# Flat mapping: field key → default value (the canvas declares one `item`).

type: "sha256"
value: ""
action: "detect"
severity: "medium"
platforms: "windows, mac, linux"
appliedGlobally: true
hostGroups: ""
expiration: ""
description: ""
tags: ""
