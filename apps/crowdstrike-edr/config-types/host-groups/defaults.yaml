# Default values seeded into every new host group item on the canvas.
# Flat mapping: field key → default value (the canvas declares one `item`).

name: ""
description: ""
groupType: "dynamic"
assignmentRule: ""
