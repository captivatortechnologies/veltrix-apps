# Default values used when creating a new host group configuration canvas.
# Keys are canvas section names; values are field defaults.

Host Group:
  name: ""
  description: ""
  groupType: "dynamic"
  assignmentRule: ""
