# Defaults for a new role item. Intentionally minimal — the field-level
# defaultValues in canvas.yaml seed the common settings (elevated_privilege and
# requires_subscription off). The operator fills in the name and description.
