# Defaults for a new script-include item. Intentionally minimal — the field-level
# defaultValues in canvas.yaml seed the common settings (active on, client_callable
# off, access=package_private). The operator fills in the name and script.
