# Defaults for a new client-script item. Intentionally minimal — the
# field-level defaultValues in canvas.yaml seed the common settings (active
# and global on, isolate_script on, type=onLoad, uiType=all, order=100). The
# operator fills in the name, table and script.
