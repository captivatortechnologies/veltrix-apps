# Defaults for a new UI-action item. Intentionally minimal — the field-level
# defaultValues in canvas.yaml seed the common settings (active on, order=100,
# form button on, show on insert/update on, isolate script on). The operator
# fills in the name, table, and the client onclick or server script.
