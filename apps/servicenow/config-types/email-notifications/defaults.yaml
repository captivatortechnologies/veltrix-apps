# Defaults for a new email-notification item. Intentionally minimal — the
# field-level defaultValues in canvas.yaml seed the common settings (active on,
# weight=0, mandatory off). The operator fills in the name, table, event name,
# recipients and message.
