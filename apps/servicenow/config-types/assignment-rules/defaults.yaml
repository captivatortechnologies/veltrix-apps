# Defaults for a new assignment-rule item. Intentionally minimal — the
# field-level defaultValues in canvas.yaml seed the common settings (active
# on, order=100). The operator fills in the name, table, condition and either
# a static group/user or a script.
