# Defaults for a new system-property item. Intentionally minimal — the
# field-level defaultValues in canvas.yaml seed the common settings
# (type=string, ignore_cache on, is_private off). The operator fills in the
# name, value and description.
