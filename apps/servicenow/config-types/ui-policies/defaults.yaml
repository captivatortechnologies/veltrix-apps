# Defaults for a new UI-policy item. Intentionally minimal — the field-level
# defaultValues in canvas.yaml seed the common behavior (ui_type=All, active,
# global, on_load and reverse_if_false on, order=100). The operator fills in the
# short description, table and condition.
