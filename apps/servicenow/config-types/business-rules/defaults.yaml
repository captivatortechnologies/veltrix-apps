# Defaults for a new business-rule item. Intentionally minimal — the field-level
# defaultValues in canvas.yaml seed the common execution settings (when=before,
# order=100, active/advanced/actionInsert on). The operator fills in the name,
# table, condition and script.
