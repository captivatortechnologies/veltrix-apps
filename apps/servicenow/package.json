{
  "name": "veltrix-app-servicenow",
  "private": true,
  "version": "0.3.0",
  "description": "Veltrix app: manage ServiceNow configuration as code — ACLs, roles, business rules, UI/data policies, script includes, client scripts, UI actions, scheduled/assignment rules, email notifications and system properties over the Table API",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@veltrixsecops/app-sdk": "^3.6.0",
    "fastify": "^5.0.0",
    "typescript": "^5.6.0"
  }
}
