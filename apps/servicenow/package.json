{
  "name": "veltrix-app-servicenow",
  "private": true,
  "version": "0.1.0",
  "description": "Veltrix app: manage ServiceNow configuration as code — business rules (sys_script) over the Table API",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@veltrixsecops/app-sdk": "^3.6.0",
    "fastify": "^5.0.0",
    "typescript": "^5.6.0"
  }
}
