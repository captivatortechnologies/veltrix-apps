{
  "name": "veltrix-app-splunk-soar",
  "private": true,
  "version": "1.2.0",
  "description": "Veltrix app: manage Splunk SOAR as code via its REST API — connection, severities, container statuses/labels, CEF custom fields, roles, assets, automation accounts and custom lists",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^20.0.0",
    "@veltrixsecops/app-sdk": "^3.7.0",
    "fastify": "^5.0.0",
    "typescript": "^5.6.0"
  }
}
