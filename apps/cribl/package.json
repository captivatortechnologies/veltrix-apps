{
  "name": "veltrix-app-cribl",
  "private": true,
  "version": "0.3.0",
  "description": "Veltrix app: manage Cribl Stream (telemetry data pipeline) as code — the full REST configuration surface",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@veltrixsecops/app-sdk": "^3.6.0",
    "fastify": "^5.0.0",
    "typescript": "^5.6.0"
  }
}
