# Defaults for a new Lookup item. The worker group and storage mode default to
# Cribl's own defaults; the operator fills in the filename-shaped id and content.
worker_group: "default"
mode: "memory"
content: ""
