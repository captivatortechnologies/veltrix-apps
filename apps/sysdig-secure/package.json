{
  "name": "veltrix-app-sysdig-secure",
  "private": true,
  "version": "0.3.0",
  "description": "Veltrix app: manage Sysdig Secure as code — threat detection, notification channels, teams, zones, CSPM posture and vulnerability management — over the Sysdig Secure REST API",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@veltrixsecops/app-sdk": "^3.6.0",
    "fastify": "^5.0.0",
    "typescript": "^5.6.0"
  }
}
