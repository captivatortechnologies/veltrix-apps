# Defaults for a new Proxy provider item — intentionally minimal; the operator
# fills in the name, flow UUIDs and external host.
mode: "proxy"
internal_host_ssl_validation: true
basic_auth_enabled: false
