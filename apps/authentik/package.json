{
  "name": "veltrix-app-authentik",
  "private": true,
  "version": "0.3.0",
  "description": "Veltrix app: manage authentik (open-source Identity Provider) as code — Applications, OAuth2/OpenID Providers, Groups and Flows over the authentik REST API, plus BYOL infrastructure provisioning for the authentik stack",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^20.0.0",
    "@veltrixsecops/app-sdk": "^3.7.0",
    "fastify": "^5.0.0",
    "typescript": "^5.6.0"
  }
}
