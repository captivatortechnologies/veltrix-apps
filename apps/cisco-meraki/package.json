{
  "name": "veltrix-app-cisco-meraki",
  "private": true,
  "version": "0.2.0",
  "description": "Veltrix app: manage Cisco Meraki Dashboard network security configuration as code — MX L3/L7 firewall rules, group policies and appliance VLANs — through the Meraki Dashboard API v1",
  "scripts": { "typecheck": "tsc --noEmit" },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^20.0.0",
    "@veltrixsecops/app-sdk": "^3.7.0",
    "fastify": "^5.0.0",
    "typescript": "^5.6.0"
  }
}
