# Defaults for a new one-to-many NAT ruleset item. Seeded with a single
# illustrative rule so a new item starts from valid, well-formed JSON.
network_id: ""
comment: ""
rules: |
  [
    {
      "publicIp": "146.11.11.13",
      "uplink": "internet1",
      "portRules": [
        { "name": "Rule 1", "protocol": "tcp", "publicPort": "9443", "localIp": "192.168.128.1", "localPort": "443", "allowedIps": ["any"] }
      ]
    }
  ]
