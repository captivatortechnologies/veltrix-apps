# Defaults for a new User Group item. The item starts with membershipMethod
# STATIC (the JumpCloud default); the operator fills in the name and, optionally,
# a description and email.
membershipMethod: "STATIC"
