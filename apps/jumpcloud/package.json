{
  "name": "veltrix-app-jumpcloud",
  "private": true,
  "version": "0.3.0",
  "description": "Veltrix app: manage JumpCloud directory, policy, network-access and organization configuration as code — 13 configuration types over the JumpCloud REST API v1/v2 (x-api-key)",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@veltrixsecops/app-sdk": "^3.7.0",
    "fastify": "^5.0.0",
    "typescript": "^5.6.0"
  }
}
