{
  "name": "veltrix-app-teleport",
  "private": true,
  "version": "0.1.0",
  "description": "Veltrix app: manage Teleport (Gravitational) infrastructure access & PAM configuration as code - RBAC roles, GitHub SSO connectors, trusted clusters, Machine ID bots, dynamic database resources, and cloud auto-discovery configs - via the Teleport Proxy web API",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^20.0.0",
    "@veltrixsecops/app-sdk": "^3.7.0",
    "fastify": "^5.0.0",
    "typescript": "^5.6.0"
  }
}
