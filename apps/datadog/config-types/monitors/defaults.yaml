# Defaults for a new Monitor item.
#
# Intentionally minimal: the per-field defaultValue in canvas.yaml already
# seeds type and options. Any values here would be a FLAT map keyed by the
# canvas field keys (name, type, query, message, priority, tags, options).
