# Defaults for a new Log Archive item.
#
# Intentionally minimal. Any values here would be a FLAT map keyed by the
# canvas field keys (name, query, rehydration_tags,
# rehydration_max_scan_size_in_gb, destination).
