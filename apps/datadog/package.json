{
  "name": "veltrix-app-datadog",
  "private": true,
  "version": "0.2.0",
  "description": "Veltrix app: manage Datadog as code — Security Monitoring rules + suppressions, Log Management pipelines, and Monitors — via the Datadog REST API",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@veltrixsecops/app-sdk": "^3.7.0",
    "fastify": "^5.0.0",
    "typescript": "^5.6.0"
  }
}
