# Defaults for a new shared device authentication item.
#
# A FLAT map keyed by the canvas field keys. Active defaults to true (matching
# Duo's own create default); name and the id lists are left blank for a
# deliberate choice.
active: true
