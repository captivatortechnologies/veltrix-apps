# Defaults for a new administrator item.
#
# A FLAT map keyed by the canvas field keys. Only the role is seeded (Read-only,
# the least-privileged), leaving email and name for a deliberate choice.
role: Read-only
