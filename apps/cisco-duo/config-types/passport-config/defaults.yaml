# Defaults for the singleton Passport configuration item.
#
# Intentionally empty — the item starts blank. Any values here would be a FLAT
# map keyed by the canvas field keys (enabled_status, enabled_groups,
# disabled_groups, custom_browsers_macos, custom_browsers_windows).
