# Defaults for a new administrative unit item.
#
# A FLAT map keyed by the canvas field keys. Both restriction flags default to
# false (unrestricted); name and description are left blank for a deliberate
# choice.
restrict_by_groups: false
restrict_by_integrations: false
