# Defaults for a new Identity Provider item.
#
# Intentionally empty: a new item starts blank. Any values here would be a flat map
# keyed by the canvas field keys (name, is_active, description, icon_ref,
# idp_entity_id, idp_login_url, idp_logout_url, idp_public_cert).
