{
  "name": "veltrix-app-auth0",
  "private": true,
  "version": "0.2.0",
  "description": "Veltrix app: manage Auth0 (Okta-owned enterprise identity platform) as code — Applications (Clients), Connections, Resource Servers (APIs) and Roles over the Auth0 Management API v2",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@veltrixsecops/app-sdk": "^3.7.0",
    "fastify": "^5.0.0",
    "typescript": "^5.6.0"
  }
}
