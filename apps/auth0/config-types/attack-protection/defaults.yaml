# Defaults for the Attack Protection singleton — intentionally empty. Every
# sub-resource field is optional and a blank field means "leave it untouched"
# (see canvas.yaml), so there is no safe non-blank default for any of them.
