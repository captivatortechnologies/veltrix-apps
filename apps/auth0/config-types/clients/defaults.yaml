# Defaults for a new Auth0 Application item — intentionally minimal; the item
# starts with the SPA application type and the operator fills in the name and
# URLs (matches other apps' item defaults).
app_type: "spa"
