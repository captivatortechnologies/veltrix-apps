# Defaults for a new AI Gateway MCP server item.
#
# A FLAT map keyed by the canvas field keys. Protocol defaults to https; name,
# host, port, path and the capability filters are left blank for a deliberate choice.
protocol: "https"
