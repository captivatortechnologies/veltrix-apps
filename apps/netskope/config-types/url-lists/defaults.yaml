# Defaults for a new URL list item.
#
# A FLAT map keyed by the canvas field keys. Only the match type is seeded
# (exact, the common case); name and entries are left blank for a deliberate
# choice.
type: exact
