# Defaults for a new destination profile item.
#
# A FLAT map keyed by the canvas field keys. Match type defaults to
# case-insensitive; name, values and labels are left blank for a deliberate choice.
type: "insensitive"
