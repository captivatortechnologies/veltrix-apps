# Defaults for a new AI Gateway provider item.
#
# A FLAT map keyed by the canvas field keys. Protocol defaults to https; name,
# schema, host, port and certificate are left blank for a deliberate choice.
protocol: "https"
