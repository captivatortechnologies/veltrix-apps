# Defaults for a new custom category item.
#
# A FLAT map keyed by the canvas field keys. Everything is left blank for a
# deliberate choice — there is no sensible default membership.
description: ""
