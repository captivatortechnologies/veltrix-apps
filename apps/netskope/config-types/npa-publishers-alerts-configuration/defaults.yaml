# Defaults for the NPA publisher alerts configuration singleton.
#
# Intentionally empty — adminUsers, eventTypes and selectedUsers are all
# required with no sensible universal default. Any values here would be a
# FLAT map keyed by the canvas field keys.
