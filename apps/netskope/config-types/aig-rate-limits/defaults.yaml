# Defaults for a new AI Gateway rate-limit rule item.
#
# A FLAT map keyed by the canvas field keys. Name, criteria, limit, appliance ids
# and response are left blank for a deliberate choice, so there are no seeded defaults.
