# Defaults for a new ingest pipeline item.
#
# Intentionally empty: the platform no longer seeds system default values into a
# new item — it starts blank. Any values here would be a FLAT map keyed by the
# canvas field keys (id, description, processorsJson, onFailureJson, version,
# metaJson), since a pipeline's presentational groups share one flat item
# record.
