# Defaults for a new transform item.
#
# Intentionally empty: the platform no longer seeds system default values into a
# new item — it starts blank. Any values here would be a FLAT map keyed by the
# canvas field keys (transformId, description, enabled, sourceIndex,
# sourceQueryJson, destIndex, destPipeline, definitionJson), since a
# transform's presentational groups share one flat item record.
