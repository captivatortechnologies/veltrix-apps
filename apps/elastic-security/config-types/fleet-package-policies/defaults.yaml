# Defaults for a new Fleet package policy item.
#
# Intentionally empty: the platform no longer seeds system default values into a
# new item — it starts blank. Any values here would be a FLAT map keyed by the
# canvas field keys (name, description, namespace, enabled, policyIds,
# packageName, packageVersion, packageTitle, inputsJson, varsJson), since a
# policy's presentational groups share one flat item record.
