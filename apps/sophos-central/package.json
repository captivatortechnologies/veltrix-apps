{
  "name": "veltrix-app-sophos-central",
  "private": true,
  "version": "0.1.0",
  "description": "Veltrix app: manage Sophos Central endpoint configuration as code through the Sophos Central APIs (endpoint policies, endpoint groups, exclusions, allow/block lists, web control local sites, custom roles, exploit mitigation)",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^20.0.0",
    "@veltrixsecops/app-sdk": "^3.7.0",
    "fastify": "^5.0.0",
    "typescript": "^5.6.0"
  }
}
