# Defaults for a new shaping profile item.
#
# A FLAT map keyed by the canvas field keys. The profile type is seeded and the
# shaping entries start as an empty JSON array; the profile name is left blank
# for a deliberate choice.
type: policing
shapingEntries: "[]"
