# Defaults for a new DNS filter profile item.
#
# A FLAT map keyed by the canvas field keys. Only the common block action and
# logging toggles are seeded; the name and advanced body are left blank for a
# deliberate choice.
blockAction: redirect
blockBotnet: true
safeSearch: false
logAllDomain: false
sdnsFtgdErrLog: true
sdnsDomainLog: true
