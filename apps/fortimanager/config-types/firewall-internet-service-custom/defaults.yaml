# Defaults for a new custom internet service item.
#
# A FLAT map keyed by the canvas field keys. The entry list starts as an empty
# JSON array; the name is left blank for a deliberate choice.
entry: "[]"
