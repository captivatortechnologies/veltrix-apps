# Defaults for a new FSSO agent item.
#
# A FLAT map keyed by the canvas field keys. Only the common agent type is seeded;
# the collector-agent host and password are left blank for a deliberate choice.
type: default
