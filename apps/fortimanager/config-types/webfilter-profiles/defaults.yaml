# Defaults for a new web filter profile item.
#
# A FLAT map keyed by the canvas field keys. Only the common logging toggles are
# seeded; the name and advanced body are left blank for a deliberate choice.
httpsReplacemsg: true
webContentLog: true
logAllUrl: false
extendedLog: false
wisp: false
