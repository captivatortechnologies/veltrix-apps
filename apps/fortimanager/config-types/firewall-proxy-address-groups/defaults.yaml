# Defaults for a new explicit-proxy address group item.
#
# A FLAT map keyed by the canvas field keys. Only the type is seeded (src, the
# most common); name and members are left blank for a deliberate choice.
type: src
