# Defaults for a new LDAP server item.
#
# A FLAT map keyed by the canvas field keys. Only the common bind type, cnid and
# connection security are seeded; the address and credentials are left blank for
# a deliberate choice.
type: simple
cnid: cn
secure: disable
