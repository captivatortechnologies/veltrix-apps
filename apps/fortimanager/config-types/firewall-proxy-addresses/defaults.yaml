# Defaults for a new explicit-proxy address item.
#
# A FLAT map keyed by the canvas field keys. Only the most common type (url) is
# seeded; the host and path are left blank for a deliberate choice.
type: url
