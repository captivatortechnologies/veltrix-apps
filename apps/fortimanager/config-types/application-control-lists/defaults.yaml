# Defaults for a new application control profile item.
#
# A FLAT map keyed by the canvas field keys. The default actions are seeded and
# the entries start as an empty JSON array; the name is left blank for a
# deliberate choice.
otherApplicationAction: pass
unknownApplicationAction: pass
entries: "[]"
