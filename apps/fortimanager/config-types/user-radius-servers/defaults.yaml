# Defaults for a new RADIUS server item.
#
# A FLAT map keyed by the canvas field keys. Only the common auth type is seeded;
# the address and shared secret are left blank for a deliberate choice.
authType: auto
