# Defaults for a new firewall IPv6 address group item.
#
# Intentionally empty — a new group starts blank. Any values here would be a FLAT
# map keyed by the canvas field keys (name, members, comment).
