# Defaults for a new user group item.
#
# A FLAT map keyed by the canvas field keys. Only the common group type is seeded;
# the name and members are left blank for a deliberate choice.
groupType: firewall
