# Defaults for a new firewall VIP group item.
#
# Intentionally empty — a new group starts blank. Any values here would be a FLAT
# map keyed by the canvas field keys (name, members, interface, comment).
