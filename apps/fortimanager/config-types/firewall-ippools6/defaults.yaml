# Defaults for a new firewall IPv6 IP pool item.
#
# Intentionally empty — name and the address range are left blank for a
# deliberate choice. Any values here would be a FLAT map keyed by the canvas
# field keys (name, startIp, endIp, comment).
