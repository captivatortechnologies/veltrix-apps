# Defaults for a new IPS sensor item.
#
# A FLAT map keyed by the canvas field keys. The botnet-scan default is seeded
# and the entries start as an empty JSON array; the name is left blank for a
# deliberate choice.
scanBotnetConnections: disable
entries: "[]"
