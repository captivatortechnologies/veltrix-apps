# Defaults for a new firewall IP pool item.
#
# A FLAT map keyed by the canvas field keys. Only the type is seeded (overload,
# the most common); name and the address range are left blank for a deliberate
# choice.
type: overload
