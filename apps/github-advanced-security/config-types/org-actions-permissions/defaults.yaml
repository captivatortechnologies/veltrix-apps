# Defaults for a new configuration item: Actions enabled everywhere, all
# actions allowed, read-only workflow permissions (GitHub's own default).
enabled_repositories: "all"
allowed_actions: "all"
github_owned_allowed: true
verified_allowed: false
patterns_allowed: []
sha_pinning_required: false
default_workflow_permissions: "read"
can_approve_pull_request_reviews: false
