# Defaults for a new configuration item. The operator fills in the
# organization and role name, picks a base role, and lists any extra
# permissions.
description: ""
base_role: "read"
permissions: []
