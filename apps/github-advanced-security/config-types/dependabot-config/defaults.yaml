# Defaults for a new repository item — both features start off; the operator
# fills in the repository (owner/repo) and toggles the desired features on.
vulnerability_alerts: false
security_updates: false
