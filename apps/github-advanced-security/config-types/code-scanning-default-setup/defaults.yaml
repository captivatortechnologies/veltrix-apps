# Defaults for a new configuration item. The operator fills in the repository
# and turns default setup on, then optionally narrows languages / query suite.
state: "not-configured"
query_suite: "default"
threat_model: "remote"
languages: []
runner_type: ""
runner_label: ""
