# Defaults for the Lifecycle Settings singleton — intentionally empty. There is
# no safe universal default for a tenant's discovery schedule; the operator
# declares exactly the top-level keys they want to control.
