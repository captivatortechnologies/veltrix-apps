# Defaults for a new user item — intentionally blank; the item starts empty and
# the operator fills in the user name and role name. `ignore_role_assignment_rules`
# seeds to false via the canvas field's defaultValue (matches other apps). There
# is deliberately no password default — see _shared.ts for why.
