# Defaults for a new saved-query item — intentionally blank; the item starts empty
# and the operator fills in the name, asset module, AQL filter and columns (matches
# other apps). `entity` seeds to "devices" via the canvas field's defaultValue.
