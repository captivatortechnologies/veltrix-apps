# Defaults for a new enforcement-set item — intentionally blank; the item starts
# empty and the operator fills in the name, main action name, config and any
# triggers (matches the saved-queries config type).
