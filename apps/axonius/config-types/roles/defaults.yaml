# Defaults for a new role item — intentionally blank; the item starts empty and
# the operator fills in the name and permissions. `data_scope_enabled` seeds to
# false via the canvas field's defaultValue (matches other apps).
