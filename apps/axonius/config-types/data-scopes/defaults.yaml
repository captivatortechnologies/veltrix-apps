# Defaults for a new data-scope item — intentionally blank; the item starts
# empty and the operator fills in the name, description and at least one
# devices/users saved-query reference (matches other apps).
