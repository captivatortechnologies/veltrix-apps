# Defaults for a new tag item — intentionally blank; the item starts empty and the
# operator fills in the tag name, asset module and AQL filter. `entity` seeds to
# "devices" via the canvas field's defaultValue (matches the saved-queries type).
