# Defaults for a new instance item — intentionally blank; the item starts
# empty and the operator fills in the node_id of an existing instance and its
# display name. `use_as_environment_name` seeds to false via the canvas
# field's defaultValue (matches other apps).
