{
  "name": "veltrix-app-axonius",
  "private": true,
  "version": "0.3.0",
  "description": "Veltrix app: manage Axonius (CAASM) as code — saved queries, enforcement sets, tags, roles, users, data scopes, instance attributes and lifecycle settings over the Axonius REST API",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@veltrixsecops/app-sdk": "^3.7.0",
    "fastify": "^5.0.0",
    "typescript": "^5.6.0"
  }
}
