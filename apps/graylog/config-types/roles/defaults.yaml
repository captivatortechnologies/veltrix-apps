# Defaults for a new role item. Empty permission set — the operator names the
# role and grants specific permissions. Flat mapping of field key → default
# (item form).
description: ""
permissions: "[]"
