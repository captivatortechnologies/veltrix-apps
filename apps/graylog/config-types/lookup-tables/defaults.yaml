# Defaults for a new lookup-table item. The operator fills in the name/title
# and the cache/data-adapter names to wire together. Flat mapping of field
# key → default (item form).
description: ""
default_single_value: ""
default_single_value_type: "NULL"
default_multi_value: ""
default_multi_value_type: "NULL"
