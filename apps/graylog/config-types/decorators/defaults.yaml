# Defaults for a new decorator item. Global scope (no stream), empty config —
# the operator fills in the type and, if scoping to one stream, the stream
# title. Flat mapping of field key → default (item form).
stream_title: ""
order: 0
config: "{}"
