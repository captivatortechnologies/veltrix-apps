// Deployment status for a lookup-data-adapters configuration, from platform
// records. Shared across every Graylog config type.
export { graylogConfigStatus as default } from '../../lib/handlerHelpers'
