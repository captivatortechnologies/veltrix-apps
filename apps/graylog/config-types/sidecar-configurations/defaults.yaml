# Defaults for a new sidecar-configuration item. The operator fills in the
# name, the collector to target and the template body. Flat mapping of field
# key → default (item form).
collector_os: ""
color: "#FF3B2F"
tags: "[]"
template: ""
