{
  "name": "veltrix-app-graylog",
  "private": true,
  "version": "0.4.0",
  "description": "Veltrix app: manage Graylog (open-source SIEM / log management) as code — streams, inputs, extractors, grok patterns, pipelines/rules/connections, index sets, lookup tables, event definitions, notifications, roles, Sidecar and outputs over the Graylog REST API, plus BYOL infrastructure provisioning for the Graylog stack",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@veltrixsecops/app-sdk": "^3.7.0",
    "fastify": "^5.0.0",
    "typescript": "^5.6.0"
  }
}
