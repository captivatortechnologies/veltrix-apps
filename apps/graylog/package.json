{
  "name": "veltrix-app-graylog",
  "private": true,
  "version": "0.1.0",
  "description": "Veltrix app: manage Graylog (open-source SIEM / log management) as code — message streams over the Graylog REST API",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@veltrixsecops/app-sdk": "^3.7.0",
    "fastify": "^5.0.0",
    "typescript": "^5.6.0"
  }
}
