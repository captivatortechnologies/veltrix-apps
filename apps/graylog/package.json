{
  "name": "veltrix-app-graylog",
  "private": true,
  "version": "0.2.0",
  "description": "Veltrix app: manage Graylog (open-source SIEM / log management) as code — streams, inputs, pipeline rules and index sets over the Graylog REST API",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@veltrixsecops/app-sdk": "^3.7.0",
    "fastify": "^5.0.0",
    "typescript": "^5.6.0"
  }
}
