{
  "name": "veltrix-app-splunk-cloud",
  "private": true,
  "version": "1.11.0",
  "description": "Veltrix app: manage Splunk Cloud Platform configurations as code \u2014 indexes, HEC tokens, IPv4/IPv6 allow lists and outbound ports, and private/Splunkbase apps via the Admin Config Service (ACS) API, and roles/users via the Splunk Cloud Platform REST API",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^20.0.0",
    "@veltrixsecops/app-sdk": "^3.1.0",
    "fastify": "^5.0.0",
    "typescript": "^5.6.0"
  }
}
