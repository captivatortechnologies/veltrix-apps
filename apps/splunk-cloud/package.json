{
  "name": "veltrix-app-splunk-cloud",
  "private": true,
  "version": "1.2.1",
  "description": "Veltrix app: manage Splunk Cloud Platform configurations as code via the Admin Config Service (ACS) API (indexes, HEC tokens, IP allow lists)",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^20.0.0",
    "@veltrixsecops/app-sdk": "^2.5.0",
    "fastify": "^5.0.0",
    "typescript": "^5.6.0"
  }
}
