# Default values seeded into every new allow list item on the canvas.
# Flat mapping: field key → default value (the canvas declares one `item`).

feature: "search-api"
subnets: []
removeUndeclared: false
