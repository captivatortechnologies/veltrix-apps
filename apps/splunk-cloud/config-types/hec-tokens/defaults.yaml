# Default values seeded into every new HEC token item on the canvas.
# Flat mapping: field key → default value (the canvas declares one `item`).

name: ""
defaultIndex: "main"
allowedIndexes: []
defaultSource: ""
defaultSourcetype: ""
useAck: false
disabled: false
