# Defaults for a new Splunk Cloud App item.
#
# Intentionally empty: the platform no longer seeds system default values into a
# new item — it starts blank (the canvas template's per-field defaultValue seeds
# the initial UI state). Users manage their own default value sets and can apply
# one when creating an item.
