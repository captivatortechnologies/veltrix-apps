# Defaults for a new limits.conf setting.
#
# Intentionally empty: a new item starts blank. Users pick a stanza + setting and
# enter the value they want to enforce; ACS ships its own defaults for any
# setting left unmanaged.
