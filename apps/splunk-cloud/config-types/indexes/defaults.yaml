# Default values seeded into every new index item on the canvas.
# Flat mapping: field key → default value (the canvas declares one `item`).

name: ""
datatype: "event"
searchableDays: 90
maxDataSizeMB: 0
splunkArchivalRetentionDays: 0
selfStorageBucketPath: ""
