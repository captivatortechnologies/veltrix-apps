{
  "name": "veltrix-app-wiz",
  "private": true,
  "version": "1.1.0",
  "description": "Veltrix app: manage Wiz (CNAPP) cloud security configuration as code — service accounts and custom cloud configuration rules — through the Wiz GraphQL API",
  "scripts": { "typecheck": "tsc --noEmit" },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^20.0.0",
    "@veltrixsecops/app-sdk": "^3.2.0",
    "fastify": "^5.0.0",
    "typescript": "^5.6.0"
  }
}
