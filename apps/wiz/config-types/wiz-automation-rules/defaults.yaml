# Defaults for a new Wiz automation rule item.
#
# Intentionally empty: the platform starts a new item blank and users manage
# their own default value sets. Any values here would be a FLAT map keyed by the
# canvas field keys (name, description, enabled, trigger_source, trigger_types,
# filters, project_id, integration_id, action_template_type,
# action_template_params).
