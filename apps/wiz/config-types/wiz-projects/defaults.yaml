# Defaults for a new Wiz project item.
#
# Intentionally empty: the platform starts a new item blank and users manage
# their own default value sets. Any values here would be a FLAT map keyed by the
# canvas field keys (name, description, business_unit, is_folder,
# parent_project_id, archived, identifiers, project_owners,
# security_champions, risk_business_impact, risk_has_authentication,
# risk_has_exposed_api, risk_is_internet_facing, risk_is_customer_facing,
# risk_stores_data, risk_is_regulated, risk_is_actively_developed,
# risk_sensitive_data_types, risk_regulatory_standards, resource_links_json).
