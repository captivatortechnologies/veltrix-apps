# Defaults for a new Wiz SAML identity provider item.
#
# Intentionally empty: the platform starts a new item blank and users manage
# their own default value sets. Any values here would be a FLAT map keyed by the
# canvas field keys (name, login_url, issuer_url, logout_url, certificate,
# use_provider_managed_roles, allow_manual_role_override,
# merge_groups_mapping_by_role, domains, group_mapping).
