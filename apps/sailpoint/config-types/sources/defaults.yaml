# Defaults for a new source item.
#
# A FLAT map keyed by the canvas field keys. Name, owner, connector and
# attributes start blank; the delete threshold defaults to 0 (ISC default).
deleteThreshold: 0
