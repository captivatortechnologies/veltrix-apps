# Defaults for a new provisioning policy item.
#
# A FLAT map keyed by the canvas field keys. Usage type defaults to CREATE;
# source name, policy name and field rules start blank.
usageType: "CREATE"
