# Defaults for a new source schema item.
#
# A FLAT map keyed by the canvas field keys. Include-permissions is off by
# default; source name, schema name and attribute definitions start blank.
includePermissions: false
