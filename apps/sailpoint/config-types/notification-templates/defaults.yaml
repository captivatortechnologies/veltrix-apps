# Defaults for a new notification template item.
#
# A FLAT map keyed by the canvas field keys. Medium defaults to EMAIL and locale
# to en; key, subject and body start blank.
medium: "EMAIL"
locale: "en"
