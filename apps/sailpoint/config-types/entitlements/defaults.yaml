# Defaults for a new entitlement overlay item.
#
# A FLAT map keyed by the canvas field keys. Source, name and attribute start
# blank; requestable, privileged and the aggregation locks start off.
requestable: false
privileged: false
lockDisplayName: false
lockDescription: false
