# Defaults for a new identity attribute item.
#
# A FLAT map keyed by the canvas field keys. Type defaults to string; multi and
# searchable are off; name and sources start blank.
type: "string"
multi: false
searchable: false
