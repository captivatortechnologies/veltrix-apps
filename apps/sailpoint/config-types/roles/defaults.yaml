# Defaults for a new role item.
#
# A FLAT map keyed by the canvas field keys. A new role is disabled and
# requestable by default; name, owner and access profiles start blank.
enabled: false
requestable: true
