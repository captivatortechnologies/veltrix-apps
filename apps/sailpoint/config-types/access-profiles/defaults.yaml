# Defaults for a new access profile item.
#
# A FLAT map keyed by the canvas field keys. A new profile is disabled and
# requestable by default; name, owner, source and entitlements start blank.
enabled: false
requestable: true
