# Defaults for a new lifecycle state item.
#
# A FLAT map keyed by the canvas field keys. A new state is enabled by default;
# profile name, technical name, access profiles and account actions start blank.
enabled: true
