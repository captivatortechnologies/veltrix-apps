# Defaults for a new Check Point service item.
#
# A FLAT map keyed by the canvas field keys. protocol and color are seeded;
# name and port are left blank for a deliberate choice.
protocol: tcp
color: black
