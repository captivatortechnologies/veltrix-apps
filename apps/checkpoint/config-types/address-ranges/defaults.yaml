# Defaults for a new Check Point address-range item.
#
# A FLAT map keyed by the canvas field keys. Only color is seeded; name and
# the address endpoints are left blank for a deliberate choice.
color: black
