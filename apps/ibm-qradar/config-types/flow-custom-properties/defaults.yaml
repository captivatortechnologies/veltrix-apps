# Defaults for a new flow-custom-property item.
#
# A FLAT map keyed by the canvas field keys. The property type defaults to
# string; the name and expressions are left for the operator to declare.
propertyType: "string"
useForRuleEngine: false
