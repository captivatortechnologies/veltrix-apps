# Defaults for a new reference map item.
#
# A FLAT map keyed by the canvas field keys. Only the value element type is
# seeded (ALN, the most general); the name and entries are left blank for a
# deliberate choice.
elementType: ALN
