# Defaults for a new log-source item.
#
# A FLAT map keyed by the canvas field keys. A log source is enabled by default;
# type, protocol and parameters are left for the operator to declare.
enabled: true
