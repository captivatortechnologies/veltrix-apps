# Defaults for a new reference table item.
#
# A FLAT map keyed by the canvas field keys. Only the default value element type
# is seeded (ALN, the most general); the name, columns and cells are left blank
# for a deliberate choice.
elementType: ALN
