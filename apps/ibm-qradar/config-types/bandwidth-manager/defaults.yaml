# Defaults for a new bandwidth-configuration item.
#
# A FLAT map keyed by the canvas field keys. Host id defaults to -1 (all hosts);
# the name and limit are left for the operator to declare.
hostId: -1
