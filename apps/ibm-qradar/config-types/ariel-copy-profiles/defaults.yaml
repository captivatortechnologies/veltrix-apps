# Defaults for a new ariel-copy-profile item.
#
# A FLAT map keyed by the canvas field keys. Enabled defaults to false (QRadar's
# own default); the name, host id and destination are left for the operator to
# declare.
enabled: false
