# Defaults for a new reference set item.
#
# A FLAT map keyed by the canvas field keys. Only the element type is seeded
# (ALN, the most general); the name and values are left blank for a deliberate
# choice.
elementType: ALN
