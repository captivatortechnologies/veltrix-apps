# Defaults for a new DDoS Allow List entry.
#
# Intentionally empty except for the canvas field default (netmask). A new
# item starts blank; any values here would be a flat map keyed by the canvas
# field keys (ip, netmask, note, allow_bypass).
