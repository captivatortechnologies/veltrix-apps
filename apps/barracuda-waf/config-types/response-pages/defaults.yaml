# Defaults for a new Response Page.
#
# Intentionally empty except for the canvas field default (type: "Error
# Pages"). A new item starts blank; any values here would be a flat map keyed
# by the canvas field keys.
