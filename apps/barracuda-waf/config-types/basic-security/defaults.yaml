# Defaults for the Basic Security singleton.
#
# Intentionally empty: a new item starts blank and the canvas field default
# (see canvas.yaml) applies. A value here would be a flat map keyed by the
# canvas field key (protection_mode).
