# Defaults for a new Header Allow/Deny rule.
#
# Intentionally empty except for the canvas field defaults (enabled, active,
# and every block_* attack-type toggle, all false). A new item starts blank;
# any values here would be a flat map keyed by the canvas field keys.
