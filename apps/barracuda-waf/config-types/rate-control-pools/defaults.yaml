# Defaults for a new Rate Control Pool.
#
# Intentionally empty except for the canvas field defaults
# (max_active_requests, max_unconfigured_clients, max_per_client_backlog). A
# new item starts blank; any values here would be a flat map keyed by the
# canvas field keys.
