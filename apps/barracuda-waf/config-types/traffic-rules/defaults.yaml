# Defaults for a new Traffic Rule.
#
# Intentionally empty except for the canvas field defaults (status, host_match,
# url_match, extended_match, extended_match_sequence). A new item starts blank;
# any values here would be a flat map keyed by the canvas field keys.
