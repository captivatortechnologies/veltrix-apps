{
  "name": "veltrix-app-runzero",
  "private": true,
  "version": "0.1.0",
  "description": "Veltrix app: manage runZero (asset discovery / attack-surface / network inventory) as code — Sites over the runZero console REST API",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@veltrixsecops/app-sdk": "^3.7.0",
    "fastify": "^5.0.0",
    "typescript": "^5.6.0"
  }
}
