# Defaults for a new User item — intentionally blank apart from Send Email Invite
# defaulting to true (matches the canvas field default); the operator fills in the
# email and role assignment. Matches other config types in this app.
