# Defaults for a new Organization item — intentionally blank; the item starts empty
# and the operator fills in the name and (optionally) hierarchy/retention settings.
# Matches other config types in this app.
