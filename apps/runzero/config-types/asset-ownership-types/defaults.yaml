# Defaults for a new Ownership Type item — intentionally blank apart from Hidden
# defaulting to false (matches the canvas field default); the operator fills in the
# name. Matches other config types in this app.
