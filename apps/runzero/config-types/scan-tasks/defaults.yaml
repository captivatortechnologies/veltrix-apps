# Defaults for a new Scan Task item — a weekly scan of the site's default scope. The
# operator sets the scan name and target site; frequency/targets are pre-filled sensibly.
targets: "defaults"
frequency: "weekly"
