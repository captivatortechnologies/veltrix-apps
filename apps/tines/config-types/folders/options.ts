// Live picker for the Folder canvas's "Team" field (optionsSource: "teams").
// Shares the provider used across every tines config type that references a
// team, keeping the per-config-type handler path the platform expects.
export { default } from '../lib/tinesOptions'
