# Defaults for a new folder item — intentionally blank except the content type,
# which is the field operators are most likely to forget to set. A FLAT map
# keyed by the canvas field keys.
content_type: "STORY"
