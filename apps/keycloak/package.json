{
  "name": "veltrix-app-keycloak",
  "private": true,
  "version": "0.1.0",
  "description": "Veltrix app: manage Keycloak (open-source Identity and Access Management) as code — OIDC/SAML clients over the Keycloak Admin REST API",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@veltrixsecops/app-sdk": "^3.7.0",
    "fastify": "^5.0.0",
    "typescript": "^5.6.0"
  }
}
