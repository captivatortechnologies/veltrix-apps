# Defaults for a new identity provider mapper item. The operator fills in the
# identity provider alias, mapper name, mapper type and config — there is no
# sensible default for a mapper type since it depends entirely on the target
# identity provider's protocol (OIDC/SAML/social).
