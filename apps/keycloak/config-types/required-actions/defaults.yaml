# Defaults for a new Keycloak required action item. The operator fills in the
# alias and name; enabled by default, not assigned to new users unless marked
# otherwise.
enabled: true
defaultAction: false
