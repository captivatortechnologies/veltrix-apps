# Defaults for a new Keycloak authentication flow item. The operator fills in the
# alias; a flow is a basic-flow unless marked otherwise. Created flows are always
# top-level and never built-in, regardless of any input.
providerId: basic-flow
