# Defaults for a new firewall alias item. `type` defaults to "host" (the most
# common alias kind); `name`, `descr`, `address` and `detail` are left blank
# for the operator to fill in.
type: host
descr: ""
