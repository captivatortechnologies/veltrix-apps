{
  "//": "Node-ESM bring-up orchestrator, invoked by the generic platform worker after tofu apply. Separate from the app's bundler-mode tsconfig because this is runnable Node code (import.meta, node: builtins, .js specifiers).",
  "compilerOptions": {
    "target": "ES2022",
    "module": "esnext",
    "moduleResolution": "bundler",
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "resolveJsonModule": true,
    "types": ["node"]
  },
  "include": ["**/*.ts"]
}
