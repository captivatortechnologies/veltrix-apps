# Default values seeded into every new role item on the canvas.
# Flat mapping: field key → default value (the canvas declares one `item`).
# Quota defaults mirror Splunk's own authorize.conf defaults.

name: ""
importedRoles: ["user"]
capabilities: ["search"]
defaultApp: ""
srchIndexesAllowed: []
srchIndexesDefault: []
srchFilter: ""
srchTimeWin: -1
srchDiskQuota: 100
srchJobsQuota: 3
rtSrchJobsQuota: 6
