# Defaults for a new cross-tenant partner item.
#
# Seeds an empty settings object (inherit the tenant default) the author can fill
# in. Values are a FLAT map keyed by the canvas field keys.
configuration: "{}"
