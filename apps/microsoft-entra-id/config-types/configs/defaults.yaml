# Defaults for a new item created from this config type.
#
# Leave this empty: the platform does not seed system default values into new
# items — a new item starts blank. Provide user-managed defaults instead, which
# users can apply when creating an item.
