# Defaults for a new administrative unit item.
#
# A new unit starts blank except for a public visibility default. Values are a
# FLAT map keyed by the canvas field keys (name, description, visibility).
visibility: "public"
