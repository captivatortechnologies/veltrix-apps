# Defaults for a new access review definition item.
#
# Seeds empty JSON scaffolds the author fills in. Values are a FLAT map keyed by
# the canvas field keys.
descriptionForAdmins: ""
scope: "{}"
reviewers: "[]"
settings: "{}"
