# Defaults for a new group item.
#
# Intentionally empty — a new group starts blank. Any values here would be a
# FLAT map keyed by the canvas field keys (name, description, mailNickname).
# mailNickname is derived from the name at deploy time when left blank, so there
# is nothing to seed.
