# Defaults for a new access package item.
#
# Seeds a visible package with no catalog set (the author supplies the catalog
# name). Values are a FLAT map keyed by the canvas field keys.
catalogName: ""
description: ""
isHidden: false
