# Defaults for a new connected organization item.
#
# Seeds a configured organization with an empty identity-sources array. Values
# are a FLAT map keyed by the canvas field keys.
description: ""
state: "configured"
identitySources: "[]"
