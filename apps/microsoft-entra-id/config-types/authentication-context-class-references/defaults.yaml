# Defaults for a new authentication context item.
#
# Seeds the first reserved id (c1), unpublished until the author is ready.
# Values are a FLAT map keyed by the canvas field keys (contextId, displayName,
# description, isAvailable).
contextId: "c1"
isAvailable: false
