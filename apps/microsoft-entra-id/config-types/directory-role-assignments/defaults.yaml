# Defaults for a new directory role assignment item.
#
# Seeds a tenant-wide ("/") assignment. Values are a FLAT map keyed by the canvas
# field keys (roleDefinitionId, principalId, directoryScopeId, label).
directoryScopeId: "/"
label: ""
