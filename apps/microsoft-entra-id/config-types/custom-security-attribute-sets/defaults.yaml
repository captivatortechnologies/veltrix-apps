# Defaults for a new custom security attribute set item.
#
# Seeds an empty description; the max-attributes limit is left blank (no limit).
# Values are a FLAT map keyed by the canvas field keys.
description: ""
