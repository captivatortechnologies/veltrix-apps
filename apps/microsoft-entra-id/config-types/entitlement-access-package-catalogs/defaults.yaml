# Defaults for a new access package catalog item.
#
# Seeds a published, internally-visible catalog. Values are a FLAT map keyed by
# the canvas field keys.
description: ""
state: "published"
isExternallyVisible: false
