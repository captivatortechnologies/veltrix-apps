# Defaults for the authentication flows policy singleton.
#
# Defaults to disabled so a freshly added item makes no change until the admin
# opts in. Values are a FLAT map keyed by the canvas field keys.
selfServiceSignUpEnabled: false
