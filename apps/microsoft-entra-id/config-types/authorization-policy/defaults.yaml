# Defaults for the tenant authorization policy singleton.
#
# All fields default to "do not manage" / off so a freshly added policy makes no
# change until the admin opts in. Values are a FLAT map keyed by the canvas field
# keys.
allowInvitesFrom: ""
guestUserRoleId: ""
allowEmailVerifiedUsersToJoinOrganization: false
allowedToUseSSPR: false
allowUserConsentForRiskyApps: false
blockMsolPowerShell: false
allowedToSignUpEmailBasedSubscriptions: false
