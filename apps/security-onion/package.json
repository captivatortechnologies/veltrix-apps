{
  "name": "veltrix-app-security-onion",
  "private": true,
  "version": "0.2.0",
  "description": "Veltrix app: manage Security Onion (NSM/SIEM) as code — Suricata rules, firewall/analyst access, SOC users, Elastic detections & ILM, Zeek — with BYOL infrastructure",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@veltrixsecops/app-sdk": "^3.5.0",
    "fastify": "^5.0.0",
    "typescript": "^5.6.0"
  }
}
