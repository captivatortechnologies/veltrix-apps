# Defaults for a new scan-config item. baseConfigId is pre-seeded to the
# feed-provided "Full and fast" config (the most common clone source); family/
# NVT selection and preferences are left blank (keep the cloned config as-is).
baseConfigId: "daba56c8-73ec-11df-a475-002264764cea"
familyGrowing: false
