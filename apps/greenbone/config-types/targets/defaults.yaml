# Defaults for a new scan-target item. The operator fills in the name and hosts;
# the port list is pre-seeded to "All IANA assigned TCP" (the most common choice)
# and can be changed per target.
portListId: "33d0cd82-57c6-11e1-8ed1-406186ea4fc5"
