# Defaults for a new role item.
#
# Any values here would be a FLAT map keyed by the canvas field keys (name,
# roleType, description, accountGroupIds, resourceListIds, restrictDismissalAccess).
restrictDismissalAccess: false
