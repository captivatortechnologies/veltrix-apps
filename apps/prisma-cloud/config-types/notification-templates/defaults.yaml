# Defaults for a new notification template item.
#
# A FLAT map keyed by the canvas field keys (name, integrationType,
# integrationId, enabled, templateConfig).
integrationType: "email"
enabled: true
