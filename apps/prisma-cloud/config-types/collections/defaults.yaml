# Defaults for a new collection item.
#
# A FLAT map keyed by the canvas field keys (name, description, accountGroupIds,
# accountIds, repositoryIds). Empty by default — every field is user-supplied.
