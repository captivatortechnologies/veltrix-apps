# Defaults for a new compliance section item.
#
# Intentionally empty — a new section starts blank. Any values here would be a
# FLAT map keyed by the canvas field keys (standardName, requirementId,
# sectionId, description, viewOrder).
