# Defaults for a new integration item.
#
# Any values here would be a FLAT map keyed by the canvas field keys (name,
# description, integrationType, enabled, accountId, regions, defaultRegion,
# orgId, sourceId).
integrationType: "aws_security_hub"
enabled: true
