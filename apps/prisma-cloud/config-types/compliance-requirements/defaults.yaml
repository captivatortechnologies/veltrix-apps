# Defaults for a new compliance requirement item.
#
# Intentionally empty — a new requirement starts blank. Any values here would be
# a FLAT map keyed by the canvas field keys (standardName, requirementId, name,
# description, viewOrder).
