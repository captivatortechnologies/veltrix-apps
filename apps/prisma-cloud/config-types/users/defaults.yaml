# Defaults for a new user item.
#
# Any values here would be a FLAT map keyed by the canvas field keys (email,
# firstName, lastName, timeZone, defaultRoleId, roleIds, accessKeysAllowed,
# enabled).
accessKeysAllowed: false
enabled: true
