# Defaults for a new Organization item. Flat mapping of field key -> default (the
# canvas template uses the "item" form). There is no other declarative field
# besides the operator-entered name, so this file is intentionally empty.
