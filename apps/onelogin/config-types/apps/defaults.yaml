# Defaults for a new app item.
#
# Intentionally empty: the platform no longer seeds default values into a new
# item - it starts blank. Any values here would be a FLAT map keyed by the
# canvas field keys (name, connectorId, description, notes, visible,
# allowAssumedSignin, policyId, tabId, provisioningEnabled, configurationJson,
# parametersJson), since an app's presentational groups share one flat item
# record.
