# Defaults for a new tag — the name, colour and description all start blank (the
# operator fills them in). Darktrace assigns a default colour when none is supplied,
# so there is nothing to seed here.
