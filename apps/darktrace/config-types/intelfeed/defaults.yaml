# Defaults for a new watched entry — the entry, source, description and expiry
# start blank (the operator fills them in). Only the two response flags default,
# matching the canvas checkbox defaults.
hostname: false
iagn: false
