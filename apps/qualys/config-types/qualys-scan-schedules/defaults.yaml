# Defaults for a new scan schedule item.
#
# Intentionally empty: a new item starts blank. Any values here would be a flat
# map keyed by the canvas field keys (scan_title, option_title,
# asset_group_titles, active, schedule_json).
