# Defaults for a new asset tag item.
#
# A new item starts as a static tag. Values are a flat map keyed by the canvas
# field keys (name, rule_type, rule_text, color, criticality_score).
rule_type: "STATIC"
