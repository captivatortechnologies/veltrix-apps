# Defaults for a new dynamic search list item.
#
# Intentionally empty: a new item starts blank. Any values here would be a flat
# map keyed by the canvas field keys (title, global, comments, criteria_json).
