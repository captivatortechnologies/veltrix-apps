# Defaults for a new authentication record item.
#
# Intentionally empty: a new item starts blank. Any values here would be a flat
# map keyed by the canvas field keys (record_type, title, ips, comments,
# credentials_json).
