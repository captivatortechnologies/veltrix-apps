# Defaults for a new VM option profile item.
#
# Intentionally empty: a new item starts blank. Any values here would be a flat
# map keyed by the canvas field keys (title, global, is_default, settings_json).
