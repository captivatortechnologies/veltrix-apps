# Defaults for a new asset group item.
#
# Intentionally empty: a new item starts blank. Any values here would be a flat
# map keyed by the canvas field keys (title, ips, business_impact, division,
# location, comments, network_id).
