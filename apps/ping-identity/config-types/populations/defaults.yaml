# Defaults for a new population item.
#
# Intentionally empty: the platform no longer seeds default values into a new
# item - it starts blank. Any values here would be a FLAT map keyed by the
# canvas field keys (name, description, default, preferredLanguage,
# alternativeIdentifiers, passwordPolicyId, defaultIdentityProviderId), since a
# population's presentational groups share one flat item record, e.g.:
#   default: false
#   preferredLanguage: en
