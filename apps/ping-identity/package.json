{
  "name": "veltrix-app-ping-identity",
  "private": true,
  "version": "0.1.0",
  "description": "Veltrix app: manage PingOne (Ping Identity cloud IAM) configuration as code - sign-on policies + actions, password policies, populations, groups, applications, resources + scopes, identity providers, MFA device policies, risk policies - via the PingOne Platform API",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^20.0.0",
    "@veltrixsecops/app-sdk": "^3.7.0",
    "fastify": "^5.0.0",
    "typescript": "^5.6.0"
  }
}
