# Defaults for a new project item — intentionally minimal. The item starts with
# tag management on (the checkbox default is declared on the canvas field); the
# operator supplies the project name and, optionally, a primary branch and tags.
manageTags: true
