# Defaults for a new Managed Scan item — both scan modes start off. The operator
# supplies the project name and turns on the scan modes they want Semgrep to run.
fullScanEnabled: false
diffScanEnabled: false
