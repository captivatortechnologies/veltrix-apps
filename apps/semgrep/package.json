{
  "name": "veltrix-app-semgrep",
  "private": true,
  "version": "0.1.0",
  "description": "Veltrix app: manage Semgrep AppSec Platform configuration as code — project settings (primary branch + tags) over the Semgrep public REST API v1",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^20.0.0",
    "@veltrixsecops/app-sdk": "^3.7.0",
    "fastify": "^5.0.0",
    "typescript": "^5.6.0"
  }
}
