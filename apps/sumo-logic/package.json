{
  "name": "veltrix-app-sumo-logic",
  "private": true,
  "version": "0.2.0",
  "description": "Veltrix app: manage Sumo Logic (cloud SIEM) as code — Field Extraction Rules, Partitions, Custom Fields and Roles over the Sumo Logic Management API",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@veltrixsecops/app-sdk": "^3.6.0",
    "fastify": "^5.0.0",
    "typescript": "^5.6.0"
  }
}
