{
  "name": "veltrix-app-sumo-logic",
  "private": true,
  "version": "0.3.0",
  "description": "Veltrix app: manage Sumo Logic (cloud SIEM) as code — Field Extraction Rules, Partitions, Fields, Roles, Monitors, Connections, Scheduled Views, Ingest Budgets, Data Forwarding, Content Folders, Dashboards, Log Searches, SAML Configuration, Users and Tokens over the Sumo Logic Management API",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@veltrixsecops/app-sdk": "^3.6.0",
    "fastify": "^5.0.0",
    "typescript": "^5.6.0"
  }
}
