# Defaults for a new data forwarding rule item — the operator fills in the index
# id and destination id. A new rule is enabled and uses the CSV file format
# unless overridden.
enabled: true
format: "csv"
