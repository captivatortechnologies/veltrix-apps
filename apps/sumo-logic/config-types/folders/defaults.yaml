# Defaults for a new folder item — the operator fills in the name and an
# existing parent folder id. No implicit default parent — Sumo Logic's Content
# Management API always requires one.
description: ""
