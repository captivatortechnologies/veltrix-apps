# Defaults for a new policy item.
#
# A FLAT map keyed by the canvas field keys. Seeds the priority level (the safe
# middle default); name, description and the policy JSON body are left blank for
# a deliberate choice.
priorityLevel: MEDIUM
