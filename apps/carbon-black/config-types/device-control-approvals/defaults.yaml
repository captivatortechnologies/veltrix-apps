# Defaults for a new device-control approval item.
#
# A FLAT map keyed by the canvas field keys. The approval name and device selector
# are left blank for a deliberate choice (this map is intentionally empty of
# device values).
{}
