# Defaults for a new asset group item.
#
# A FLAT map keyed by the canvas field keys. Seeds the member type (the only
# supported value); name, description, query and policy are left blank for a
# deliberate choice.
memberType: DEVICE
