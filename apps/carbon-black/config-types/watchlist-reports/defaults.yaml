# Defaults for a new watchlist report item.
#
# A FLAT map keyed by the canvas field keys. Seeds the severity and IOC field
# (SHA256 hashes, the common case); title, description and values are left blank
# for a deliberate choice.
severity: 5
iocField: process_hash
