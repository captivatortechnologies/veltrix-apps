# Defaults for a new notification integration item — intentionally blank; the
# item starts empty and the operator picks a service and fills in that
# service's fields (matches the other Orca config types). Field-level
# defaultValue in canvas.yaml seeds service, isEnabled, isDefault and
# slackShowActions/webhookType.
