# Defaults for a new custom alert item — intentionally blank; the item starts
# empty and the operator fills in the name, category, score and Sonar query
# (matches other apps). Field-level defaultValue in canvas.yaml seeds category,
# score and the checkboxes.
