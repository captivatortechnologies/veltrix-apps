# Defaults for a new discovery alert item — intentionally blank; the item
# starts empty and the operator fills in the name, category, score and
# Discovery query (matches the other Orca config types). Field-level
# defaultValue in canvas.yaml seeds category, score and the context flag.
