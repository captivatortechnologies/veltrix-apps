# Defaults for a new custom tag rule item — intentionally blank; the item
# starts empty and the operator fills in the name, tags and query (matches the
# other Orca config types). Field-level defaultValue in canvas.yaml seeds
# "disabled" and "ruleType".
