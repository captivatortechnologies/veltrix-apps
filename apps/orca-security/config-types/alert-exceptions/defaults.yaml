# Defaults for a new alert exception item — intentionally blank; the item
# starts empty and the operator fills in the rule_id and desired enabled state
# (matches the other Orca config types). The "Enabled" checkbox defaults to
# true via canvas.yaml's field-level defaultValue.
