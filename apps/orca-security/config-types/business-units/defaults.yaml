# Defaults for a new business unit item — intentionally blank; the item starts
# empty and the operator fills in the name, ownership metadata and scope filter.
# Field-level defaultValue in canvas.yaml seeds the criticality/filter selects
# and the checkbox.
