# Defaults for a new automation item — intentionally blank; the item starts
# empty and the operator fills in the name, Sonar query and actions. Field-level
# defaultValue in canvas.yaml seeds the status select to "enabled".
