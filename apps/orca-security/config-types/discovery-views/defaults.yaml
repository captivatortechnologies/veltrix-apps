# Defaults for a new discovery view item — intentionally blank; the item starts
# empty and the operator fills in the name and Discovery query. Field-level
# defaultValue in canvas.yaml seeds the view type and the sharing checkbox.
