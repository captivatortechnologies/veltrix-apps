# Defaults for a new trusted cloud account item — intentionally blank; the
# item starts empty and the operator fills in the account name, description,
# provider and account id (matches the other Orca config types).
