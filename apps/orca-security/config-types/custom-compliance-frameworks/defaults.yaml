# Defaults for a new custom compliance framework item — intentionally blank;
# the item starts empty and the operator fills in the name, description and
# sections JSON (matches the other Orca config types).
