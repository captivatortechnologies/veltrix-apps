# Defaults for a new Twingate Resource item.
#
# Intentionally empty: the platform starts a new item blank and users manage
# their own default value sets. Any values here would be a FLAT map keyed by
# the canvas field keys (name, address, remote_network_name, alias, is_visible,
# is_browser_shortcut_enabled, allow_icmp, tcp_policy, tcp_ports, udp_policy,
# udp_ports, group_names).
