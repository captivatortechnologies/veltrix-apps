# Defaults for a new Twingate DNS Filtering Profile item.
#
# Intentionally empty: the platform starts a new item blank and users manage
# their own default value sets. Any values here would be a FLAT map keyed by
# the canvas field keys (name, priority, fallback_method, allowed_domains,
# denied_domains, content_categories, security_categories, privacy_categories,
# group_names).
