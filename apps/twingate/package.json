{
  "name": "veltrix-app-twingate",
  "private": true,
  "version": "0.3.0",
  "description": "Veltrix app: manage Twingate (ZTNA / Zero Trust Network Access) configuration as code — Resources, Remote Networks, Groups, Service Accounts, Connectors and DNS Filtering Profiles — through the Twingate GraphQL API",
  "scripts": { "typecheck": "tsc --noEmit" },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^20.0.0",
    "@veltrixsecops/app-sdk": "^3.7.0",
    "fastify": "^5.0.0",
    "typescript": "^5.6.0"
  }
}
