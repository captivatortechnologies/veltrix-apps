# Defaults for a new log-stream item.
#
# Intentionally empty: the platform no longer seeds system default values into a
# new item — it starts blank. Any values here would be a FLAT map keyed by the
# canvas field keys (name, type, status, settingsJson, splunkToken), since a
# stream's presentational groups share one flat item record, e.g.:
#   type: aws_eventbridge
#   status: ACTIVE
