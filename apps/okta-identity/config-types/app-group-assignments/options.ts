// Options provider for the App Group Assignments config type — thin re-export of the shared
// provider so this type's remote-select fields resolve their live options.
export { default } from '../lib/oktaOptions'
