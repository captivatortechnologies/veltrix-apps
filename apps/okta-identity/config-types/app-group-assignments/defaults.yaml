# Defaults for a new app-group assignment item.
#
# Intentionally empty: the platform no longer seeds default values into a new
# item — it starts blank. Any values here would be a FLAT map keyed by the canvas
# field keys (appId, groupId, priority, profileJson), since an assignment's one
# presentational group shares a single flat item record, e.g.:
#   appId: ""
#   groupId: ""
