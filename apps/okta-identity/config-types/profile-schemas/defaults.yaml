# Defaults for a new profile-schema item.
#
# Intentionally empty: the platform no longer seeds system default values into a
# new item — it starts blank. Any values here would be a FLAT map keyed by the
# canvas field keys (schemaType, userTypeId, attributesJson), since a profile
# schema's presentational groups share one flat item record, e.g.:
#   schemaType: user
#   userTypeId: default
