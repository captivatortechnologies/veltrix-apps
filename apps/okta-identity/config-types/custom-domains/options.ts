// Options provider for the Custom Domains config type — thin re-export of the
// shared provider so this type's remote-select field (Brand, source: "brands")
// resolves its live options.
export { default } from '../lib/oktaOptions'
