# Defaults for a new brand item.
#
# Intentionally empty: the platform no longer seeds system default values into a
# new item — it starts blank. Any values here would be a FLAT map keyed by the
# canvas field keys (name, removePoweredByOkta, customPrivacyPolicyUrl,
# agreeToCustomPrivacyPolicy, locale, emailDomainId, primaryColorHex,
# primaryColorContrastHex, secondaryColorHex, secondaryColorContrastHex,
# themeConfigJson).
