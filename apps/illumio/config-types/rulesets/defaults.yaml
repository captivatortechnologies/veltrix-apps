# Defaults for a new ruleset item.
#
# A FLAT map keyed by the canvas field keys. Only `enabled` is seeded (true,
# matching the canvas field's own defaultValue) — name, scope and rules are
# left blank for a deliberate choice.
enabled: true
