# Defaults for a new label item.
#
# A FLAT map keyed by the canvas field keys. No field has a universally sensible
# default — key/value are the label's identity and externalDataSet /
# externalDataReference are purely optional integration metadata — so this is
# intentionally empty; every new item starts blank.
{}
