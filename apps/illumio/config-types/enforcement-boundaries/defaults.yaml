# Defaults for a new enforcement boundary item.
#
# A FLAT map keyed by the canvas field keys. Only `enabled` is seeded (true,
# matching the canvas field's own defaultValue) — name, providers, consumers
# and services are left blank for a deliberate choice.
enabled: true
