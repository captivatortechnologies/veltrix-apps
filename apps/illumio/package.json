{
  "name": "veltrix-app-illumio",
  "private": true,
  "version": "0.3.0",
  "description": "Veltrix app: manage Illumio Core (PCE) microsegmentation configuration as code — labels, IP lists, services, label groups, virtual services, rulesets, enforcement boundaries and pairing profiles",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@veltrixsecops/app-sdk": "^3.7.0",
    "fastify": "^5.0.0",
    "typescript": "^5.6.0"
  }
}
