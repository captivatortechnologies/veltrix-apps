{
  "name": "veltrix-app-illumio",
  "private": true,
  "version": "0.2.0",
  "description": "Veltrix app: manage Illumio Core (PCE) microsegmentation configuration as code — labels, IP lists, services and rulesets (label-scoped security rules) over the Illumio REST API v2's draft-then-provision model",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@veltrixsecops/app-sdk": "^3.7.0",
    "fastify": "^5.0.0",
    "typescript": "^5.6.0"
  }
}
