// Options provider for zone settings. Zone-scoped, so it powers the canvas-level
// "Domain" picker (source = "zones") via the shared Cloudflare provider.
export { default } from '../lib/cloudflareOptions'
