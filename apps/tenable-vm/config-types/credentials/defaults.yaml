# Defaults for a new credential item.
#
# Intentionally empty: the platform no longer seeds system default values into a
# new item — it starts blank. Any values here would be a FLAT map keyed by the
# canvas field keys (name, description, type, settingsJson), since a credential's
# presentational groups share one flat item record.
#
# Deliberately NOT seeding settingsJson: it carries write-only secrets and there
# is no safe non-empty default to suggest.
