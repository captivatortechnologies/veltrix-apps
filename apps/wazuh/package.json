{
  "name": "veltrix-app-wazuh",
  "private": true,
  "version": "0.5.0",
  "description": "Veltrix app: manage Wazuh (SIEM/XDR/HIDS) as code — 10 config types (CDB lists, agent groups, custom rules/decoders, manager configuration, API users/roles/policies/RBAC rules, API security settings) over the Wazuh REST API (55000) — with BYOL cluster infrastructure groundwork",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@veltrixsecops/app-sdk": "^3.6.0",
    "fastify": "^5.0.0",
    "typescript": "^5.6.0"
  }
}
