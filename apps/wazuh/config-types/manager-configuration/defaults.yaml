# Defaults for the Manager Configuration singleton — intentionally blank
# (beyond the label default already set in canvas.yaml); the operator supplies
# the full ossec.conf body (matches other apps).
configLabel: "ossec.conf (manager)"
comment: ""
ossecConfXml: ""
