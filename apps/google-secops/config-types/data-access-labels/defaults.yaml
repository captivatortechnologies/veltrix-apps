# Defaults for a new data access label item.
#
# Intentionally minimal — a new label starts blank. Any values here would be a
# FLAT map keyed by the canvas field keys (name, udmQuery, description). The
# name and UDM query are left blank for a deliberate choice per label.
