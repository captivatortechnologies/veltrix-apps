# Defaults for a new data table item.
#
# Intentionally minimal — a new table starts blank. Any values here would be a
# FLAT map keyed by the canvas field keys (name, description, columns, rows).
# The column schema and rows are left blank for a deliberate choice.
