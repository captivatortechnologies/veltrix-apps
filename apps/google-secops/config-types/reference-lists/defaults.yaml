# Defaults for a new reference list item.
#
# A FLAT map keyed by the canvas field keys. Only the syntax type is seeded
# (plain, the most general); the name and entries are left blank for a deliberate
# choice.
syntax: plain
