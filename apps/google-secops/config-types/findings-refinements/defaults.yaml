# Defaults for a new findings refinement item.
#
# A FLAT map keyed by the canvas field keys. A new refinement starts with no
# outcome filters; display name and query are left blank for a deliberate choice.
outcomeFilters: "[]"
