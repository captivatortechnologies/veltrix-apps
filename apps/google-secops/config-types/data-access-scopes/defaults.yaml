# Defaults for a new data access scope item.
#
# Intentionally minimal — a new scope starts blank. Any values here would be a
# FLAT map keyed by the canvas field keys (name, allowAll, allowedLabels,
# deniedLabels, description). The label sets are left blank for a deliberate
# choice per scope.
