# Defaults for a new watchlist item.
#
# A FLAT map keyed by the canvas field keys. A new watchlist starts unpinned with
# a neutral multiplying factor of 1; name and description are left blank for a
# deliberate choice per watchlist.
multiplyingFactor: 1
pinned: false
