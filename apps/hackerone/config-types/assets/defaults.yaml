# Defaults for a new asset item. Keys are FLAT (item form) and must match the
# canvas field keys. asset_type/identifier have no sensible default — the
# operator must declare the actual asset.
asset_type: "domain"
max_severity: "none"
confidentiality_requirement: "none"
integrity_requirement: "none"
availability_requirement: "none"
