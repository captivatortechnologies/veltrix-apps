# Defaults for a new credential-inquiry item. The operator fills in the program
# handle, the asset identifier (an existing structured scope) and the description.
# Keys are FLAT (item form) and must match the canvas field keys.
description: ""
