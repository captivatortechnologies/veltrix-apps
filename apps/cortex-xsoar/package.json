{
  "name": "veltrix-app-cortex-xsoar",
  "private": true,
  "version": "1.1.0",
  "description": "Veltrix app: manage Cortex XSOAR (lists, incident types, jobs) configuration as code through the XSOAR REST API",
  "scripts": { "typecheck": "tsc --noEmit" },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^20.0.0",
    "@veltrixsecops/app-sdk": "^3.2.0",
    "fastify": "^5.0.0",
    "typescript": "^5.6.0"
  }
}
