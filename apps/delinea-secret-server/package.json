{
  "name": "veltrix-app-delinea-secret-server",
  "private": true,
  "version": "0.3.0",
  "description": "Veltrix app: manage Delinea Secret Server (PAM) folders, secret policies, groups, users, Distributed Engine sites, connection managers, IP address restrictions and Distributed Engine configuration as configuration code over the Secret Server REST API",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@veltrixsecops/app-sdk": "^3.7.0",
    "fastify": "^5.0.0",
    "typescript": "^5.6.0"
  }
}
