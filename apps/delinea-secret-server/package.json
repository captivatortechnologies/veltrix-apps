{
  "name": "veltrix-app-delinea-secret-server",
  "private": true,
  "version": "0.2.0",
  "description": "Veltrix app: manage Delinea Secret Server (PAM) folders, secret policies and groups as configuration code over the Secret Server REST API",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@veltrixsecops/app-sdk": "^3.7.0",
    "fastify": "^5.0.0",
    "typescript": "^5.6.0"
  }
}
