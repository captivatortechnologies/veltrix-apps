# Defaults for a new secret policy item. The operator fills in the policy name
# (and an optional description); a new policy is active by default, matching
# Secret Server's default for a newly created policy.
active: true
