{
  "name": "veltrix-app-cisco-ise",
  "private": true,
  "version": "0.2.0",
  "description": "Veltrix app: manage Cisco Identity Services Engine (ISE) network access control configuration as code — endpoint identity groups, network device groups, network devices and authorization profiles — through the ISE External RESTful Services (ERS) API",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@veltrixsecops/app-sdk": "^3.7.0",
    "fastify": "^5.0.0",
    "typescript": "^5.6.0"
  }
}
