# Defaults for a new watchlist item.
#
# Intentionally empty — a new item starts from the canvas field defaultValues
# (provider=Custom, header lines to skip=0). Any values here would be a FLAT map
# keyed by the canvas field keys (alias, display_name, items_search_key, ...).
