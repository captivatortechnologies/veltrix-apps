# Defaults for the Fusion rule item.
#
# Intentionally empty — a new item starts from the canvas field defaultValues
# (label="Fusion (Advanced Multi-Stage Attack Detection)", enabled=true). Any
# values here would be a FLAT map keyed by the canvas field keys (label, enabled).
