# Defaults for a new product-setting item.
#
# Intentionally empty — a new item starts from the canvas field defaultValues
# (setting Ueba, enabled true). Any values here would be a FLAT map keyed by the
# canvas field keys (setting, enabled, entity_providers, data_sources).
