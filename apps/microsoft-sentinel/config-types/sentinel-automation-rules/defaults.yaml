# Defaults for a new automation rule item.
#
# Intentionally empty — a new item starts from the canvas field defaultValues
# (enabled=true, order=1, triggers on Incidents when Created). Any values here
# would be a FLAT map keyed by the canvas field keys (rule_name, order, ...).
