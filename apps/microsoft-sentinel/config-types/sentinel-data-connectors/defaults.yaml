# Defaults for a new data connector item.
#
# Intentionally empty — a new item starts from the canvas field defaultValues
# (kind AzureActiveDirectory, alerts enabled). Any values here would be a FLAT map
# keyed by the canvas field keys (connector_id, kind, tenant_id, ...).
