# Defaults for a new Microsoft Security rule item.
#
# Intentionally empty — a new item starts from the canvas field defaultValues
# (enabled=true, productFilter=Microsoft Cloud App Security). Any values here
# would be a FLAT map keyed by the canvas field keys (rule_name, product_filter,
# severities_filter, ...).
