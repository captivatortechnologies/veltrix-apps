# Defaults for a new hunting query / saved search item.
#
# Intentionally empty — a new item starts from the canvas field defaultValues
# (category "Hunting Queries"). Any values here would be a FLAT map keyed by the
# canvas field keys (saved_search_name, category, query, ...).
