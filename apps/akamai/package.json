{
  "name": "veltrix-app-akamai",
  "private": true,
  "version": "0.2.0",
  "description": "Veltrix app: manage Akamai edge security as code — Network Lists + activation over the Network Lists API v2 and Client Lists over the Client Lists API v1, authenticated with EdgeGrid (EG1-HMAC-SHA256)",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@veltrixsecops/app-sdk": "^3.6.0",
    "fastify": "^5.0.0",
    "typescript": "^5.6.0"
  }
}
