# Defaults for a new directory-mapping item.
#
# Intentionally empty: a new item starts blank (canvas field defaultValues
# still apply in the editor). Any values here would be a flat map keyed by the
# canvas field keys (directory_name, mapping_name, domain_groups, ldap_branch,
# vault_groups, mapping_authorizations, location, authentication_method,
# user_type, disable_user, user_activity_log_period, user_expiration,
# logon_from_hour, logon_to_hour).
