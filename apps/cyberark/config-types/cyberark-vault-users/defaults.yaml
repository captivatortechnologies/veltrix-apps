# Defaults for a new Vault-user item.
#
# Intentionally empty: a new item starts blank (canvas field defaultValues
# still apply in the editor). Any values here would be a flat map keyed by the
# canvas field keys (username, user_type, description, location,
# initial_password, authentication_method, vault_authorization,
# unauthorized_interfaces, enable_user, change_pass_on_next_logon,
# password_never_expires, expiry_date, contact_details).
