# Defaults for a new application item.
#
# Intentionally empty: a new item starts blank (canvas field defaultValues
# still apply in the editor). Any values here would be a flat map keyed by the
# canvas field keys (app_id, description, location, disabled,
# access_permitted_from_hour, access_permitted_to_hour, expiration_date,
# business_owner_first_name, business_owner_last_name, business_owner_email,
# business_owner_phone, authentication_methods).
