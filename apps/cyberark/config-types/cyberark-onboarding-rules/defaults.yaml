# Defaults for a new onboarding-rule item.
#
# Intentionally empty: a new item starts blank (canvas field defaultValues still
# apply in the editor). Any values here would be a flat map keyed by the canvas
# field keys (rule_name, rule_description, target_platform_id, target_safe_name,
# system_type_filter, machine_type_filter, account_category_filter,
# is_admin_id_filter, user_name_filter, user_name_method, address_filter,
# address_method).
