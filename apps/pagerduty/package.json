{
  "name": "veltrix-app-pagerduty",
  "private": true,
  "version": "0.4.0",
  "description": "Veltrix app: manage PagerDuty incident-response configuration as code — escalation policies, services, schedules, teams, users, business services, event orchestrations, tags, automation actions, extensions and webhook subscriptions over the PagerDuty REST API v2",
  "scripts": { "typecheck": "tsc --noEmit" },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@veltrixsecops/app-sdk": "^3.6.0",
    "fastify": "^5.0.0",
    "typescript": "^5.6.0"
  }
}
