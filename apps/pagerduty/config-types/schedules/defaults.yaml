# Defaults for a new schedule item — intentionally blank; the item starts empty and
# the operator fills in the name, time zone and rotation layers. Any values here
# would be a FLAT map keyed by the canvas field keys (name, time_zone, schedule_layers).
