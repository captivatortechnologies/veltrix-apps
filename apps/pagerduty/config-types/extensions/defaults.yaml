# Defaults for a new extension item — intentionally blank; the item starts empty
# and the operator fills in the name, extension schema and target services.
# Any values here would be a FLAT map keyed by the canvas field keys
# (name, extension_schema, endpoint_url, extension_objects, config).
