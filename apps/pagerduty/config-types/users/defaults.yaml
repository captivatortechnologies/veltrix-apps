# Defaults for a new user item — intentionally blank; the item starts empty and
# the operator fills in the name and email. Any values here would be a FLAT map
# keyed by the canvas field keys (name, email, role, job_title, time_zone,
# description, color).
