# Defaults for a new escalation policy item — intentionally blank; the item starts
# empty and the operator fills in the name and escalation rules (matches other apps).
# Any values here would be a FLAT map keyed by the canvas field keys
# (name, description, num_loops, escalation_rules).
