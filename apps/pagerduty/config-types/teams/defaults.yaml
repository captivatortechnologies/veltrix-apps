# Defaults for a new team item — intentionally blank; the item starts empty and the
# operator fills in the name (and an optional description). Any values here would be
# a FLAT map keyed by the canvas field keys (name, description).
