# Defaults for a new business service item — intentionally blank; the item starts
# empty and the operator fills in the name (and optional description, point of
# contact and owning team). Any values here would be a FLAT map keyed by the
# canvas field keys (name, description, point_of_contact, team).
