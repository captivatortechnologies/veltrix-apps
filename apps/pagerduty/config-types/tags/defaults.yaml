# Defaults for a new tag item — intentionally blank; the item starts empty and the
# operator fills in the label (and any optional assignments). Any values here would
# be a FLAT map keyed by the canvas field keys (label, assignments).
