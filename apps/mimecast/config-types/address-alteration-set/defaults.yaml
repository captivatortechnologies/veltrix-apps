# Defaults for a new address alteration set item.
#
# A FLAT map keyed by the canvas field keys. A set has only its identity
# (description) and an optional parent, so there are no meaningful seed values —
# both are left for a deliberate choice.
{}
