# Defaults for a new directory group item.
#
# A FLAT map keyed by the canvas field keys. A group's identity (description)
# and members are left for a deliberate choice, so the seed is an empty member
# list.
members: []
