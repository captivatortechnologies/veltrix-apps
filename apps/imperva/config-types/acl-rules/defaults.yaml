# Defaults for a new ACL rule item — the operator fills in the Site ID, name and
# filter. `action` and `enabled` seed to match the canvas field defaults.
action: "RULE_ACTION_BLOCK"
enabled: "enabled"
