{
  "name": "veltrix-app-imperva",
  "private": true,
  "version": "0.3.0",
  "description": "Veltrix app: manage Imperva Cloud WAF (Incapsula) edge security as code — ACL/delivery rules, data centers, rule exceptions and site configuration over the Cloud WAF management API v1 (api_id + api_key)",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@veltrixsecops/app-sdk": "^3.7.0",
    "fastify": "^5.0.0",
    "typescript": "^5.6.0"
  }
}
