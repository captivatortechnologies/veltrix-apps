# Defaults for a new Jamf Pro building item.
#
# Intentionally empty: the platform starts a new item blank. Any values here
# would be a FLAT map keyed by the canvas field keys (name, street_address_1,
# street_address_2, city, state_province, zip_postal_code, country).
