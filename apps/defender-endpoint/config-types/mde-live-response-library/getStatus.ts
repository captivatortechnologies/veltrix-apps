import { getIndicatorStatus } from '../../lib/indicators'

// Reports deployment status against mde-tenant components (the shared, tool-wide
// status reporter — not indicator-specific).
export default getIndicatorStatus
