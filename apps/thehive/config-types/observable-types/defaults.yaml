# Defaults for a new observable-type item. A string-valued observable type by
# default; the operator sets the name and toggles isAttachment for file types.
# Keys are FLAT (item form) and must match canvas.yaml.
isAttachment: false
