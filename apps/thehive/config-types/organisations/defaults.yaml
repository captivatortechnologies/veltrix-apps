# Defaults for a new organisation item. Seeds TheHive's own defaults (manual
# sharing, unlocked); the operator fills in the name and description. Keys are
# FLAT (item form) and must match canvas.yaml.
taskRule: "manual"
observableRule: "manual"
locked: false
