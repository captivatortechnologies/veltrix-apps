# Defaults for a new user item. Seeds the common analyst profile; the operator
# fills in the login, name and (optionally) organisation. Keys are FLAT (item
# form) and must match canvas.yaml.
profile: "analyst"
