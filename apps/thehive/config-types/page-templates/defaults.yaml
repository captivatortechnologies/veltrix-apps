# Defaults for a new page-template item. Seeds order 0 (top of its category);
# the operator fills in the title, category and content. Keys are FLAT (item
# form) and must match canvas.yaml.
order: 0
