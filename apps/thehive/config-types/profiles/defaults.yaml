# Defaults for a new profile item. Left minimal on purpose — permissions are
# never seeded by default (least privilege) — the operator fills in the name
# and permission set. Keys are FLAT (item form) and must match canvas.yaml.
permissions: ""
