{
  "name": "veltrix-app-thehive",
  "private": true,
  "version": "0.2.0",
  "description": "Veltrix app: manage TheHive (open-source SIRP / SOAR) as code — case templates, custom fields, observable types and users over the TheHive REST API (Bearer API key)",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@veltrixsecops/app-sdk": "^3.7.0",
    "fastify": "^5.0.0",
    "typescript": "^5.6.0"
  }
}
