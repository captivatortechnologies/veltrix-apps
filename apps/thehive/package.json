{
  "name": "veltrix-app-thehive",
  "private": true,
  "version": "0.1.0",
  "description": "Veltrix app: manage TheHive (open-source SIRP / SOAR) as code — case templates over the TheHive REST API (Bearer API key)",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@veltrixsecops/app-sdk": "^3.7.0",
    "fastify": "^5.0.0",
    "typescript": "^5.6.0"
  }
}
