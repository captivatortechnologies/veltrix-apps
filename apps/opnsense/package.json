{
  "name": "veltrix-app-opnsense",
  "private": true,
  "version": "0.2.0",
  "description": "Veltrix app: manage OPNsense open-source firewall configuration as code through the OPNsense REST API — firewall aliases, categories, pf rules and source NAT, staged then applied",
  "scripts": { "typecheck": "tsc --noEmit" },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^20.0.0",
    "@veltrixsecops/app-sdk": "^3.7.0",
    "fastify": "^5.0.0",
    "typescript": "^5.6.0"
  }
}
