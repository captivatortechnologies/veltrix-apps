# Defaults for a new OPNsense alias item.
#
# A FLAT map keyed by the canvas field keys. name/content are left blank for a
# deliberate choice; type and enabled mirror the canvas field defaultValues.
type: host
enabled: true
