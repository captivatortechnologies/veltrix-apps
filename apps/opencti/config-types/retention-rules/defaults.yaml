# Defaults for a new retention rule item — the item starts effectively blank; the
# operator fills in the name, scope and max retention. `retention_unit` defaults to
# "days" and `active` defaults to true via their canvas defaultValue.
