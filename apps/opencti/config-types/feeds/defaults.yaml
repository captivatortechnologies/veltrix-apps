# Defaults for a new feed item — include_header defaults to true via the
# canvas; the operator fills in the name, separator, date attribute, rolling
# time, entity types and attribute mappings (matches other apps).
