# Defaults for a new kill-chain-phase item — the item starts effectively blank
# (order defaults to 0 via the canvas); the operator fills in the kill chain
# name and phase name (matches other apps).
