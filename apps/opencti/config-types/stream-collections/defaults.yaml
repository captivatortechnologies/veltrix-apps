# Defaults for a new stream collection item — the item starts effectively blank;
# the operator fills in the name and, optionally, a description and filters.
# `stream_live` defaults to true and `stream_public` defaults to false via their
# canvas defaultValue.
