# Defaults for a new TAXII collection item — the item starts effectively blank;
# the operator fills in the name and, optionally, a description and filters.
# `taxii_public`, `include_inferences` and `score_to_confidence` all default to
# false via their canvas defaultValue.
