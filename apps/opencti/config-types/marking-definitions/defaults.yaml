# Defaults for a new marking-definition item — the item starts effectively blank
# (type defaults to TLP, order to 0 via the canvas); the operator fills in the
# definition value and, optionally, a color (matches other apps).
