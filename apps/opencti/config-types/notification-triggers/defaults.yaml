# Defaults for a new notification-trigger item — event_types defaults to
# ["create"] and instance_trigger to false via the canvas; the operator fills
# in the name and, optionally, filters, recipients and notifier names.
