# Defaults for a new TAXII2 feed item — version defaults to v21 and authentication
# to none via the canvas; the operator fills in the name, URL and collection (and a
# secret only when the scheme requires one). Matches other apps.
