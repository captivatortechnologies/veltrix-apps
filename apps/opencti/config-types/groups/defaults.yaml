# Defaults for a new group item — the item starts effectively blank (both toggles
# default to false via the canvas); the operator fills in the name and, optionally,
# a description (matches other apps).
