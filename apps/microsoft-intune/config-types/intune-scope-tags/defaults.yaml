# Defaults for a new role scope tag item.
#
# Intentionally empty: a new item starts blank (name + description are free text
# with no seeded values). Any values here would be a flat map keyed by the canvas
# field keys (name, description).
