{
  "name": "veltrix-app-cybereason",
  "private": true,
  "version": "0.3.0",
  "description": "Veltrix app: manage the Cybereason Defense Platform as code — custom reputations, sensor groups, isolation rules, sensor policies and sensor tags — over the Cybereason REST API",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@veltrixsecops/app-sdk": "^3.7.0",
    "fastify": "^5.0.0",
    "typescript": "^5.6.0"
  }
}
