# Defaults for a new sensor tag-set item — intentionally blank beyond the
# select default declared in canvas.yaml; the operator fills in the pylumId.
# FLAT mapping of field key → default value (item template).
