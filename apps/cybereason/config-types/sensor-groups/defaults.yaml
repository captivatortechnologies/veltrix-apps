# Defaults for a new sensor group item — intentionally blank; the operator fills
# in the name (and optionally a description / policy id). FLAT mapping of field
# key → default value (item template).
