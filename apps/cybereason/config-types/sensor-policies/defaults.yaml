# Defaults for a new sensor policy item — intentionally blank; the operator
# fills in the name and pastes/authors the JSON configuration. FLAT mapping of
# field key → default value (item template).
