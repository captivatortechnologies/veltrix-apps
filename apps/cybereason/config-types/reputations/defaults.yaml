# Defaults for a new custom reputation item — intentionally blank beyond the
# select defaults declared in canvas.yaml; the operator fills in the key. FLAT
# mapping of field key → default value (item template).
