# Defaults for a new isolation rule item — beyond the select/checkbox defaults
# declared in canvas.yaml (direction=ALL, blocking=true), the operator fills in
# the IP address. FLAT mapping of field key → default value (item template).
