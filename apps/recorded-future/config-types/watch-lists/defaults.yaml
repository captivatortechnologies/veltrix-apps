# Defaults for a new Watch List item — intentionally blank; the item starts empty
# and the operator fills in the list name and entities (the type select default is
# declared on the canvas field itself).
