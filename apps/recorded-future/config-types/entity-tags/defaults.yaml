# Defaults for a new Entity Tagging item — intentionally blank; the item starts
# empty and the operator fills in the list name, entity reference and tag set (the
# match-by select default is declared on the canvas field itself).
