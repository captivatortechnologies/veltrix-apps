# Defaults for a new custom-compliance-rule item — intentionally blank; the item
# starts empty and the operator fills in the name and JSON logic (the select /
# checkbox defaults — risk level, enabled — are declared on the canvas fields
# themselves).
