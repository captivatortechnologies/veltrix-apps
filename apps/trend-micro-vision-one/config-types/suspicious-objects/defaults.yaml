# Defaults for a new suspicious-object item — intentionally blank; the item starts
# empty and the operator fills in the value (the select defaults — type, scan
# action, risk level — are declared on the canvas fields themselves).
