# Defaults for a new user-account item — intentionally blank; the item starts
# empty and the operator fills in the email (the select defaults — auth type,
# status — are declared on the canvas fields themselves).
