# Defaults for a new custom-script item — intentionally blank; the item starts
# empty and the operator fills in the file name and contents (the file-type select
# defaults on the canvas field itself).
