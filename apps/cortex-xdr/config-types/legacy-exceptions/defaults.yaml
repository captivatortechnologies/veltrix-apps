# Defaults for a new legacy-exception item — intentionally blank; the item starts
# empty and the operator fills in the name, module id and conditions (the select
# defaults are declared on the canvas fields themselves).
