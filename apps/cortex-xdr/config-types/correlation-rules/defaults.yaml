# Defaults for a new correlation-rule item — intentionally blank; the item starts
# empty and the operator fills in the name and XQL query (the select defaults are
# declared on the canvas fields themselves).
