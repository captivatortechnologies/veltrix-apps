# Defaults for a new syslog-integration item — intentionally mostly blank; the
# item starts empty and the operator fills in the name and address (the select /
# number defaults are declared on the canvas fields themselves).
