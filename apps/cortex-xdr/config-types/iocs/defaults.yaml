# Defaults for a new indicator item — intentionally blank; the item starts empty
# and the operator fills in the indicator value and type (the select defaults are
# declared on the canvas fields themselves).
