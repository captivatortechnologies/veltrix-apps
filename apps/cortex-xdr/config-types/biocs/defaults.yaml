# Defaults for a new BIOC rule item — intentionally blank; the item starts empty
# and the operator fills in the name and category (the select defaults are
# declared on the canvas fields themselves).
