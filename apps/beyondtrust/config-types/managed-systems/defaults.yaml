# Defaults for a new managed-system item — intentionally mostly blank; the
# item starts empty and the operator fills in the workgroup, system name and
# platform (matches the other apps). Auto-management defaults on, matching
# Password Safe's own default posture for a new managed system.
autoManagementFlag: true
accountNameFormat: 0
