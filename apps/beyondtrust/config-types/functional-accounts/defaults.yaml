# Defaults for a new functional account item — intentionally blank; the item
# starts empty and the operator fills in the platform ID, account name and the
# optional domain / presentation fields (matches the other apps).
